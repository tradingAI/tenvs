version: "3.0"

services:
  test:
    image: aiminders/python:3.8.7
    environment:
      TUSHARE_TOKEN: ${TUSHARE_TOKEN}
    command: sh /root/tenvs/docker-compose.sh
    volumes:
      - .:/root/tenvs
